name: 04 - Using Actions

on:
    workflow_dispatch:

jobs:
    build:
        runs-on: self-hosted
        steps:
            - name: "Checkout"
              uses: actions/checkout@v4

            - name: "Show folders"
              run: |
                  pwd
                  ls -la