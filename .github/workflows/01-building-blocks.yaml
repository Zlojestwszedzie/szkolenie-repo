name: 01 - Building Blocks
on: 
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello step
        run: echo "Hello, world!"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run: |
          echo "I will fail now"
          exit 0
      - name: Goodbye step
        run: |
          echo "Do widzenia!"