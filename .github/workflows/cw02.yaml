name: 02 Workflow events
on:
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Trigger
              run: |
                echo "Triggered by ${{ github.event_name }}"